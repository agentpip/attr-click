# Active Context

**State:** 2026-07-25 PUBLIC OPEN-SOURCE LINKS DEPLOYED AND CANARIED / MAIN HAS PUBLIC-SAFE DEPLOYMENT TEMPLATES / APACHE CUTOVER PENDING PRIVILEGED APPLY

**Delivered:** invitation-only passwordless access; short links with durable source-query contract; owner-scoped dashboard with privacy-safe aggregate UTM reporting; public redirects and first-party click events; canonical SVG/PNG QR codes with palettes, templates, and private logo uploads; magic-login links with explicit session logout; adaptive creator/public UI; Laravel Gate-protected admin dashboard with creator role management, invitation issuance/revocation, and polling global stats; live production runtime behind Caddy at `https://attr.click`; deployed creator-owned short-link/QR deletion; deployed canonical/Open Graph/Twitter homepage share metadata with a public 1200×630 PNG social card; deployed public `/help` user guide and README reference; public-safe deployment templates that require local target/path environment variables before sync.

**Current slice:** PR #11 merged to `main` as `80e4dc8` and was deployed with the Envoy `deploy` story. It makes the homepage footer and public `/help` page link directly to the canonical `msitarzewski/attr-click` repository and its MIT License, adds regression coverage for both public surfaces, and corrects public Composer/README metadata. GitHub confirms the public repository is MIT licensed. Public source/license links, `/`, `/help`, `/up`, `/login`, guest `/dashboard` and `/admin` redirects, and loopback health/help pass; deployed source hashes match the release. No production user/link/session/click-event mutations were used for validation.

**Current focus:** The canonical public repository is `https://github.com/msitarzewski/attr-click`; its root MIT License is recognized by GitHub, and direct source/license links are live on the homepage and help page. The production application directory is an rsync deployment artifact without a Git remote, not a fork; `80e4dc8` is the last production application deployment and its relevant source hashes match. Caddy remains the TLS edge and reverse-proxies `attr.click` to a private loopback listener; the prepared Apache vhost uses that same listener and Laravel `public/` document root. Apache 2.4 with `mod_php`/`mod_rewrite` is already the shared system runtime. Candidate Apache configuration passes remote syntax validation, but installation, reload, and the narrowly scoped `www-data` runtime ACL need interactive sudo on the production host. The next actual deployment must set `ATTR_CLICK_DEPLOY_HOST` and `ATTR_CLICK_DEPLOY_PATH` locally before Envoy runs. Once the Apache cutover is green, run `php artisan links:regenerate-qr` and replace externally printed/downloaded legacy QR images as needed.

**Security remediation, 2026-07-24:** The source uses one `DestinationUrlPolicy` for creation/editing, rejecting non-public HTTP(S) targets and hostnames whose DNS records are unresolved or non-public. Redirect analytics persists only bounded standard UTM values; it still forwards the complete lossless query contract. Passwordless URL credentials do not enter session flash, and production with `MAIL_MAILER=log` refuses to issue them. Global response middleware sets CSP, frame, content-type, referrer, permissions, cross-origin, and secure-request HSTS headers while removing the framework-version header. Post-deploy public canaries confirmed headers, HTTPS Vite assets, and guest auth redirects; no production accounts, links, sessions, or click events were created.

**Constraints:** SQLite locally; MySQL in production. Preserve URL/query behavior. Keep the initial domain model small and owner-scoped; workspace/multi-domain abstractions are deferred. Caddy is the trusted loopback proxy for production URL generation. Production uses a real SMTP transport and passwordless email delivery is working; do not expose its credentials. The private loopback listener is a production-server service port, not a local SMTP/mail-capture endpoint. QR images are rendered dynamically rather than persisted as static per-link files; reissuing cannot alter old physical QR prints that are already distributed. Avoid production probes that create users, links, sessions, or click events without explicit approval.
