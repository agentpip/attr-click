# Active Context

**State:** BUILD / Phase 2 QR styling

**Delivered:** invitation-only passwordless access; short links with durable source-query contract; owner-scoped dashboard; public redirects and first-party click events; canonical SVG QR codes; per-link foreground/background palettes; saved owner-scoped QR palette templates.

**Current focus:** Logo upload with server-side processing, richer QR module/eye styles, QR downloads, and reporting UI.

**Constraints:** SQLite locally; production targets UMBP MySQL. Preserve URL/query behavior. Keep the initial domain model small and owner-scoped; workspace/multi-domain abstractions are deferred.
