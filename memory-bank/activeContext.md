# Active Context

**State:** 2026-07-24 MAIN-ONLY CREATOR LINK-DELETION RELEASE DEPLOYED AND CANARIED / APACHE CUTOVER PENDING PRIVILEGED APPLY

**Delivered:** invitation-only passwordless access; short links with durable source-query contract; owner-scoped dashboard with privacy-safe aggregate UTM reporting; public redirects and first-party click events; canonical SVG/PNG QR codes with palettes, templates, and private logo uploads; magic-login links with explicit session logout; adaptive creator/public UI; Laravel Gate-protected admin dashboard with creator role management, invitation issuance/revocation, and polling global stats; live UMacBookPro production runtime behind Caddy at `https://attr.click`; deployed creator-owned short-link/QR deletion.

**Current slice:** PR #2 merged to `main` as `bf03ccf` and was deployed with the Envoy `deploy` story. The owner-only deletion route requires the exact slug before deleting a short link, its dynamic QR resolution, private QR logo, and cascade-owned click analytics; other creators receive `403`, and a mismatched confirmation preserves the link. Envoy rebuilt and synchronized the source/assets, Composer install, migration check, and Laravel caches all passed. Post-deploy file hashes for the controller, routes, and link-detail view match the deployed source; the route table contains `DELETE links/{link:slug}`; public HTTPS, CSP/HSTS/Vite-asset, and loopback health canaries passed. No production user/link/session/click-event mutations were used for validation.

**Current focus:** `origin/main` release `bf03ccf` was deployed with the Envoy `deploy` story on 2026-07-24. Caddy remains the TLS edge and reverse-proxies `attr.click` to loopback port 8092; the prepared Apache vhost uses that same port and Laravel `public/` document root. Apache 2.4 with `mod_php`/`mod_rewrite` is already the shared system runtime. Candidate Apache configuration passes remote syntax validation, but installation, reload, and the narrowly scoped `www-data` runtime ACL need interactive sudo on UMacBookPro. Once the Apache cutover is green, run `php artisan links:regenerate-qr` and replace externally printed/downloaded legacy QR images as needed. Envoy activates application code without restarting the current user-owned development server.

**Security remediation, 2026-07-24:** The source uses one `DestinationUrlPolicy` for creation/editing, rejecting non-public HTTP(S) targets and hostnames whose DNS records are unresolved or non-public. Redirect analytics persists only bounded standard UTM values; it still forwards the complete lossless query contract. Passwordless URL credentials do not enter session flash, and production with `MAIL_MAILER=log` refuses to issue them. Global response middleware sets CSP, frame, content-type, referrer, permissions, cross-origin, and secure-request HSTS headers while removing the framework-version header. Post-deploy public canaries confirmed headers, HTTPS Vite assets, and guest auth redirects; no production accounts, links, sessions, or click events were created.

**Constraints:** SQLite locally; UMacBookPro MySQL in production. Preserve URL/query behavior. Keep the initial domain model small and owner-scoped; workspace/multi-domain abstractions are deferred. Caddy is the trusted loopback proxy for production URL generation. Production uses a real SMTP transport and passwordless email delivery is working; do not expose its credentials. Port 8092 is a production-server service port, not a local SMTP/mail-capture endpoint. QR images are rendered dynamically rather than persisted as static per-link files; reissuing cannot alter old physical QR prints that are already distributed. Avoid production probes that create users, links, sessions, or click events without explicit approval.
