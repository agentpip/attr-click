# Active Context

**State:** SOURCE SECURITY REMEDIATION, APACHE CUTOVER, AND ADMIN SLICE REVIEW-READY / PRIVILEGED PRODUCTION APPLY AND CANARIES PENDING

**Delivered:** invitation-only passwordless access; short links with durable source-query contract; owner-scoped dashboard with privacy-safe aggregate UTM reporting; public redirects and first-party click events; canonical SVG/PNG QR codes with palettes, templates, and private logo uploads; magic-login links with explicit session logout; adaptive creator/public UI; Laravel Gate-protected admin dashboard with creator role management, invitation issuance/revocation, and polling global stats; live UMacBookPro production runtime behind Caddy at `https://attr.click`.

**Current slice:** Laravel-native `access-admin` Gate authorization protects the `/admin` area and its JSON stats endpoint. The admin sidebar item uses the same ability. Admins can see global creator/link/scan counts refreshed every 30 seconds, grant/revoke other creators’ admin roles (but cannot remove their own access), and issue/revoke hashed invitation codes. The auth login route follows Laravel’s standard `login` route-name contract, fixing guest redirects to the sign-in form; authenticated creators can explicitly log out through a CSRF-protected route that invalidates their session and rotates the CSRF token.

**Current focus:** Review and deploy the 2026-07-23 security remediation with the QR lifecycle slice and replace the production `artisan serve` runtime. Caddy remains the TLS edge and reverse-proxies `attr.click` to loopback port 8092; the prepared Apache vhost uses that same port and Laravel `public/` document root. Apache 2.4 with `mod_php`/`mod_rewrite` is already the shared system runtime. Candidate Apache configuration passes remote syntax validation, but installation, reload, and the narrowly scoped `www-data` runtime ACL need interactive sudo on UMacBookPro. Once production is green, run `php artisan links:regenerate-qr` and replace externally printed/downloaded legacy QR images as needed. Envoy now activates application code without restarting the retired user-owned development server.

**Security remediation, 2026-07-23:** The source now uses one `DestinationUrlPolicy` for creation/editing, rejecting non-public HTTP(S) targets and hostnames whose DNS records are unresolved or non-public. Redirect analytics persists only bounded standard UTM values; it still forwards the complete lossless query contract. Passwordless URL credentials no longer enter session flash, and production with `MAIL_MAILER=log` refuses to issue them. Global response middleware sets CSP, frame, content-type, referrer, permissions, cross-origin, and secure-request HSTS headers while removing the framework-version header. Local feature coverage passes; production has not yet been deployed or re-canaryed, so the prior live missing-header and `/dashboard` 500 observations remain unclosed until the deployment check.

**Constraints:** SQLite locally; UMacBookPro MySQL in production. Preserve URL/query behavior. Keep the initial domain model small and owner-scoped; workspace/multi-domain abstractions are deferred. Caddy is the trusted loopback proxy for production URL generation. Production uses a real SMTP transport and passwordless email delivery is working; do not expose its credentials. Port 8092 is a production-server service port, not a local SMTP/mail-capture endpoint. QR images are rendered dynamically rather than persisted as static per-link files; reissuing cannot alter old physical QR prints that are already distributed. Avoid production probes that create users, links, sessions, or click events without explicit approval.
